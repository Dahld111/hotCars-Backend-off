{
  "name": "hotcars-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "node --env-file .env --watch ./src/index.mjs",
    "test": "cross-env NODE_OPTIONS=--experimental-vm-modules jest",
    "test:bash": "NODE_OPTIONS=--experimental-vm-modules jest",
    "test:cmd": "set NODE_OPTIONS=--experimental-vm-modules && jest",
    "test:powershell": "$env:NODE_OPTIONS=--experimental-vm-modules; jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.16.3"
  },
  "devDependencies": {
    "cross-env": "^10.0.0",
    "jest": "^30.0.5"
  }
}
